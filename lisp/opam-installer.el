;;; opam-installer --- Stub installer for use-package

;;; Commentary:
;; This module is meant to be used by 'use-package' with the
;; :ensure-system-package attribute as in the following example:
;;
;; (use-package ruby-mode
;;   :ensure-system-package
;;   ((rubocop     . "gem install rubocop")
;;    (ruby-lint   . "gem install ruby-lint")
;;    (ripper-tags . "gem install ripper-tags")
;;    (pry         . "gem install pry")))


;;; Code:

(provide 'opam-installer)

;;; opam-installer.el ends here
